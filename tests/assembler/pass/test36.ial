field global int32 bar;

function void volatile_insn()
{
    register int32 x;

    block entry
    {
        x = load.i32 (42);
        volatile field.global.set x (bar);
        volatile leave;
    }
}
